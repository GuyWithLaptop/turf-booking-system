# Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Examples:
#   Local: postgresql://postgres:password@localhost:5432/turf_booking
#   Neon: Get from https://neon.tech (free tier available)
#   Supabase: Get from https://supabase.com (free tier available)
#   Railway: Get from https://railway.app (free trial available)
DATABASE_URL="postgresql://postgres:password@localhost:5432/turf_booking?schema=public"

# NextAuth Configuration
# For development, you can use the values below
# For production, generate a new secret: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="development-secret-change-in-production-use-openssl-rand-base64-32"

# Contact Information
# This phone number appears on the public homepage
MANAGER_PHONE="+91 9876543210"

# Optional: Node Environment
NODE_ENV="development"
